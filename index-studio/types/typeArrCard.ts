export type itemCard = {
    id: string,
    date: number
    oldPrice: string,
    price: string,
    title: string,
    seen: boolean,
    locality: string,
}
export type typeArrCard = Array<itemCard>
